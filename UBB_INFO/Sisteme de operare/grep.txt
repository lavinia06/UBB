4.Folosind utilitarul GREP afisati pe ecran toate linile unui fisier care
contin cel mult 3 vocale mari si nu contin cifre impare.i ultima aparitie a caracterului '^'
de pe toate linile dintr-un fisier.
	 grep -E "^[^13579]*$" grep.c | grep -E "(.*[AEIOU].*){,3}" -la fel cu 2


3.Folosind utilitarul GREP afisati pe ecran toate linile unui fisier care
incep cu un numar par de cifre impare si se termina cu un numar impar de
cifre pare.
	 grep -E --colour "^([13579][13579])*([^13579]{1}.*[^13579]{1})*([02468][02468])*[02468]$" grep.c

	
1. Folosind utilitarul GREP afisati pe ecran toate liniile dintr-un fisier care
contin un numar de caractere divizibil cu 3
	grep -E --colour  "^(...)+$" grep.c

9. Using grep display all the lines from a file that don't contain any letter or digit.
	 grep -E --colour "^([^0-9])*$" grep.c | grep -E --colour "^[^a-zA-Z]*$"

6. Using grep display how many lines from a file contains only big letters.
	 grep -E --count "^[A-Z]+$" grep.c

7. Using grep display the lines and the line index of thoese lines that start and end with 2
digits.
		grep -E -n  --colour "^[0-9]{2}([^0-9].*[^0-9])*[0-9]{2}$" grep.c

8. Using grep display all the characters of each line from a file after the last
aparition of the '$', character includig the '$'.
	
	grep -o --colour "\$[^\$]*$" grep.c
	grep -E -o --colour "[\$][^\$]*$" grep.c

5. Folosid utilitarul GREP fisati pe ecran toate caraterele care apar intre
prima aparitie a caracterului '|' si ultima aparitie a caracterului '^'
de pe toate linile dintr-un fisier.
	 grep -E -o --colour "[|].*[\^][^ \^]*$" grep.c
	-----asta da intre ultima aparitie al lui | si ultima al lui ^
	 grep -E -o --colour "[|][^ \|]*[\^][^ \^]*$" grep.c
	


