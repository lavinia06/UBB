# This is a sample Python script.

# Press Shift+F10 to execute it or replace it with your code.
# Press Double Shift to search everywhere for classes, files, tool windows, actions, and settings.


def maxim(int a, int b, int c)[
    int maximum == find_maxim(num1, num2, num3).

# Print the result
    write_console("The maximum number is:", maximum).
]

def minim(int a, int b, int c)[
    int minim == find_minim(num1, num2, num3).

# Print the result
    write_console("The maximum number is:", minim).
]

def prime(int a)[
    bool is_prime == check_prime(a).

    if(is_prime == true)[
        write_console("prime number").
    ]
    else
    [write_console("non prime number").]

]

def sum(int n)[
    while(n>0)[
        list list_n == empty.
        list_n.append(n).
        n == n-1.
    ]

    int i==0.
    while(i<n)[
        int suma ==0 #syntax error! the execution line must end with .
        suma == list_n[i]+suma. #wrong call of function
        i == i+1.
        ]
# Print the result
    write_console("The sum is:", suma).
]


def main()[
    write_console(maxim(1, 2, 3)).
    write_console(minim(1, 2, 3)).
    write_console(sum(3)).

]

    PROGRAM_OUT.
